#pragma once
// figmalib — render Figma files as game UI.
// Pipeline: Figma REST JSON → node tree → ThorVG scene → RGBA buffer → engine texture.

#include "document.h"
#include "parser.h"
#include "renderer.h"
#include "ui.h"
